# Production Strapi (build & runtime). For local Strapi, copy to .env.local and use http://localhost:1338
VITE_STRAPI_URL=https://api.thehumanistai.com

# Public site URL used when generating sitemap.xml (no trailing slash)
SITE_URL=https://thehumanistai.com
# Optional override for sitemap generation (defaults to VITE_STRAPI_URL)
# STRAPI_URL=https://api.thehumanistai.com
